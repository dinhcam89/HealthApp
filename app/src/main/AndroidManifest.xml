<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- MAKE PHONE CALL PERMISSION -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name="._activity.ShowClinicInfoActivity"
            android:exported="false" />
        <activity
            android:name="._activity.BookingListActivity"
            android:exported="false" />
        <activity
            android:name="._activity.CreateProfileActivity"
            android:exported="false" />
        <activity
            android:name="._activity.AppointmentDateChoosingActivity"
            android:exported="true" />
        <!--
        <activity
            android:name="._activity.AppointmentDateChoosingActivity"
            android:exported="true" />
        -->
        <activity
            android:name="._activity.MenuActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".MainActivity2" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_main2" -->
        <!-- android:theme="@style/Theme.HealthApp" /> -->
        <!-- <activity -->
        <!-- android:name=".XPK" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_xpk" -->
        <!-- android:theme="@style/Theme.HealthApp" /> -->
        <activity
            android:name="._activity.MainActivity"
            android:exported="false" />
        <activity
            android:name="._activity.MainMenuActivity"
            android:exported="false" />
        <activity
            android:name="._activity.DoctorChoosingActivity"
            android:exported="true" />
        <activity
            android:name="._activity.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="._activity.SignUpActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>